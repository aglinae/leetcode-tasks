var filter = function (arr, fn) {
    let returnedArray =[];
    arr.forEach(element => {
        
    });
};
let arr = [0, 10, 20, 30],
  fn = function greaterThan10(n) {
    return n > 10;
  };
const newArray = filter(arr, fn);
